<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keludstats">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".modul.KeludStatsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KeludStats">

        <activity
            android:name=".modul.dashboard.DashboardActivity"
            android:theme="@style/SplashTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modul.login.LoginActivity" />
        <activity android:name=".modul.table.TableActivity"/>
        <activity android:name=".modul.nilaipertahun.NilaiPerTahunActivity" />
        <activity android:name=".modul.aboutapp.AboutAppActivity" />
        <activity android:name=".modul.infografislist.InfografisListActivity" />
        <activity android:name=".modul.detailinfografi.DetailInfografiActivity" />
        <activity android:name=".modul.createinfografi.CreateInfografiActivity" />
    </application>
</manifest>